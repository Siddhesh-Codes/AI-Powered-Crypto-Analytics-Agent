# LSTM Model Requirements - Real Machine Learning Training
# Install with: pip install -r requirements_lstm.txt
# Compatible with Python 3.14

# Core ML Frameworks (Using scikit-learn - compatible with Python 3.14)
# Note: TensorFlow not yet available for Python 3.14
# We'll use RandomForest and GradientBoosting for real ML instead

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Time Series (ARIMA alternative to LSTM)
statsmodels>=0.14.0

# Utilities
requests>=2.31.0

# Already installed (from main requirements.txt)
# No need to reinstall if already present
